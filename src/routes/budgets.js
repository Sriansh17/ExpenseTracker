const express=require('express'); const controller=require('../controllers/budgetController'); const {authenticate}=require('../middlewares/authenticate'); const {validateRequest}=require('../middlewares/validateRequest'); const schemas=require('../validation/budgetSchemas'); const router=express.Router(); router.use(authenticate); router.get('/',controller.list); router.post('/monthly',validateRequest(schemas.monthly,'body'),controller.setMonthly); router.post('/category',validateRequest(schemas.category,'body'),controller.setCategory); router.get('/status',controller.getStatus); module.exports=router;
