const express=require('express'); const c=require('../controllers/notificationController'); const {authenticate}=require('../middlewares/authenticate'); const {validateRequest}=require('../middlewares/validateRequest'); const s=require('../validation/notificationSchemas'); const r=express.Router(); r.use(authenticate); r.get('/',validateRequest(s.list,'query'),c.list); r.patch('/:id/read',validateRequest(s.id,'params'),c.markRead); r.patch('/read-all',c.markAllRead); module.exports=r;
