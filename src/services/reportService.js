const q=require('../queries/reportQueries'); const {stringify}=require('csv-stringify/sync'); const PDFDocument=require('pdfkit'); async function rows(u,s,e){return q.getTransactionsInRange(u,s,e)} async function generateCsv(u,s,e){const data=await rows(u,s,e);return stringify(data,{header:true,columns:['date','type','amount','notes']})} async function generatePdf(u,s,e){const data=await rows(u,s,e);return new Promise(resolve=>{const doc=new PDFDocument();const chunks=[];doc.on('data',c=>chunks.push(c));doc.on('end',()=>resolve(Buffer.concat(chunks)));doc.fontSize(16).text('Expense Tracker Report').fontSize(10).text(`${s} to ${e}`).moveDown();data.forEach(x=>doc.text(`${x.date} | ${x.type} | ${Number(x.amount).toFixed(2)} | ${x.notes||''}`));doc.end()})} module.exports={generateCsv,generatePdf};
