const Joi=require('joi'); const currencies=require('../constants/currencies'); module.exports={currency:Joi.object({currency:Joi.string().valid(...currencies).required()}),theme:Joi.object({theme:Joi.string().valid('light','dark','system').required()}),profile:Joi.object({displayName:Joi.string().trim().min(1).max(100).required()}),password:Joi.object({password:Joi.string().required()})};
